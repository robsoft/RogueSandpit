# Map width alignment — brief spec

- The native playfield is 800 pixels wide and cells are 10 pixels wide.
- The generated map therefore contains 80 columns and fills the canvas edge-to-edge.
- The existing 58-row map plus 20-pixel HUD continues to fill the 600-pixel native height.
- Generation topology, reachability, fog, resizing, scaling, and mouse mapping remain valid.

