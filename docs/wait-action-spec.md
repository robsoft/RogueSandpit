# Wait action — brief spec

- Period (`.`) and numpad 5 explicitly spend one player turn without moving.
- NPCs take their normal response turn, so waiting can be tactically dangerous.
- The event log records `PLAYER WAITS`.
- Restart remains bound to Space and waiting is ignored after game-over.

