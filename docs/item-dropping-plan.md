# Item dropping — brief plan

1. Add safe player inventory removal that handles equipped weapons.
2. Add a drop command using the existing ground-item model.
3. Bind `D`, add event messages, and cover success/failure/equipment cases.
4. Update project notes, run the full suite and runtime smoke test, then commit.

