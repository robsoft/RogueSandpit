# Remappable input and persistent settings plan

1. Add `InputAction`, binding slots, defaults, lookup, remapping, conflict, clear, and reset models with tests.
2. Refactor gameplay and directional input translation to use actions.
3. Extend application navigation with a Controls screen and safe capture state.
4. Add laptop-friendly Wait defaults.
5. Add a resilient JSON settings store for runtime options and bindings.
6. Save after successful changes and load before constructing the real-time timer.
7. Update controls documentation, build, run the full suite, and smoke-test remapping and restart persistence.
