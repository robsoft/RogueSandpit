# Map width alignment — brief plan

1. Correct the map width from 79 to 80 columns.
2. Expose native canvas dimensions as constants and assert exact map/canvas alignment.
3. Run multi-seed generation and the full regression suite, then smoke-test at scale 1.

