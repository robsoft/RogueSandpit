# Bandages — brief spec

- Bandages are inventory consumables that remove bleeding and restore up to 12 health.
- `B` uses the selected bandage. It is consumed and costs a turn only if bleeding is removed or health is restored.
- Bandages do not remove stunned and can be dropped or thrown like ordinary items.

