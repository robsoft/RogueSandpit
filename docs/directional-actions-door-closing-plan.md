# Directional actions and door closing — brief plan

1. Add framework-independent directional close-door commands and adjacency queries.
2. Add contextual `C` handling with a reusable direction-selection state.
3. Render the active action prompt and preserve normal Escape behavior outside it.
4. Test immediate, selected, invalid, noisy, and no-door outcomes.

