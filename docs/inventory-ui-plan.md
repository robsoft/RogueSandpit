# Inventory UI — brief plan

1. Add inventory-panel state and context-sensitive input routing to `GameWrapper`.
2. Render an eight-slot panel using the existing pixel font and primitive drawing system.
3. Show selection, item attributes, equipment markers, and contextual controls.
4. Add input/state tests where practical, update controls, run the suite and smoke-test.

